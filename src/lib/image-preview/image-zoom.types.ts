export type ImageZoomTransform = {
  scale: number;
  x: number;
  y: number;
};

export type ImageZoomPoint = {
  x: number;
  y: number;
};

export type ImageZoomSize = {
  width: number;
  height: number;
};
